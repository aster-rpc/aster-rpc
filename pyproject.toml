[build-system]
requires      = ["maturin>=1.6,<2"]
build-backend = "maturin"

[project]
name    = "iroh-python"
version = "0.1.0"
description = "Python bindings for the Iroh peer-to-peer networking library"
requires-python = ">=3.9"
dependencies = []

[tool.maturin]
module-name    = "iroh_python._iroh_python"
manifest-path  = "iroh_python_rs/Cargo.toml"
features       = ["pyo3/extension-module"]
python-source  = "."

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]